import { ExitSiteDirective } from './exit-site.directive';

describe('ExitSiteDirective', () => {
  it('should create an instance', () => {
    const directive = new ExitSiteDirective();
    expect(directive).toBeTruthy();
  });
});
